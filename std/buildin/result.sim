pub struct Result<T, E>{
    data: <T, E>
}

pub func (self: Result<T, E>) is_ok()bool{
    return self.data is T
}

pub func (self: Result<T, E>) is_err()bool{
    return self.data is E
}
