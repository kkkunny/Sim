struct Option<T>{
    v: *?T
}

func (Option<T>) default()Self{
    return Self{v: null}
}

func main(){
    let v: Option::<u8> = Option::<u8>{v: null}.default()
    if v.v == null{
        debug("null")
    }
}