struct Option<T>{
    v: *?T
}

func (Option<T>) default()Self{
    return Self{v: null}
}

func main(){
    let v: Option::<u8> = Option::<u8>:default()
    if v.v == null{
        debug("null")
    }
}