func main(){
    let a: <i32, u32, f32> = 1 as i32
    match a{
    is i32:
        if a is i32{
            debug("i32")
        }
    is u32:
        debug("u32")
    other:
        debug("other")
    }
}