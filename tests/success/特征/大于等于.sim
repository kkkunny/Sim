import std::os

type A i32

func (A) gt(self: Self, dst: Self)bool{
	return true
}

func (A) eq(self: Self, dst: Self)bool{
	return true
}

func main(){
	let mut v: A = 1
	if v >= 2{
		os::exit(0)
	}
	os::exit(1)
}