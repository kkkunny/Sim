import std::c

type S struct {
	name: str
}

let name2 | S = "234"

let getname | S = (self: S) -> str {
	return self.name
}

let main = () {
	let s = S{name: "123"}
	c::puts(s.getname(s))
	c::puts(s.name2)
}
