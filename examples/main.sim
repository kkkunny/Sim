import std::c

type S struct {
	name: str
}

let name2 | S = "234"

let getname | S = (self: &S) -> str {
	return self.name
}

let main = () {
	let s = S{name: "123"}
	let s2 = &s
	c::puts(s2.getname(&s))
	c::puts(s2.name2)
}
