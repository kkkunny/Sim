// import std::io
// import std::collection::vec
//
// func add<T: Add>(l: T, r: T)T{
// 	return l + r
// }
//
// func main(){
// 	io::printf::<usize>("%lld\n", add::<usize>(1, 2))
// }
import std::os

type A i32

func (A) eq(self: Self, dst: Self)bool{
	return true
}

func main(){
	let mut v: A
	if v != 1{
		os::exit(1)
	}
	os::exit(0)
}